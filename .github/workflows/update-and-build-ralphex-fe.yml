name: Update and Build ralphex-fe

on:
  workflow_dispatch:
    inputs:
      bun_version:
        description: 'New Bun version (e.g., 1.3.9). Leave empty to keep current.'
        required: false
        type: string
      hugo_version:
        description: 'New Hugo version (e.g., 0.155.3). Leave empty to keep current.'
        required: false
        type: string
      ralphex_version:
        description: 'Ralphex version / Docker image tag (e.g., 0.11.0). Leave empty to keep current.'
        required: false
        type: string
      update_only:
        description: 'Only update Dockerfile without building image'
        required: false
        default: false
        type: boolean

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ralphex-fe
  DOCKERFILE_PATH: ralphex-fe/Dockerfile

jobs:
  update-dockerfile:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    outputs:
      versions_updated: ${{ steps.update.outputs.updated }}
      bun_version: ${{ steps.update.outputs.bun_version }}
      hugo_version: ${{ steps.update.outputs.hugo_version }}
      ralphex_version: ${{ steps.update.outputs.ralphex_version }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update Dockerfile versions
        id: update
        env:
          INPUT_BUN_VERSION: ${{ inputs.bun_version }}
          INPUT_HUGO_VERSION: ${{ inputs.hugo_version }}
          INPUT_RALPHEX_VERSION: ${{ inputs.ralphex_version }}
        run: |
          # Read current versions
          CURRENT_BUN=$(grep '^ARG BUN_VERSION=' "${{ env.DOCKERFILE_PATH }}" | cut -d'=' -f2)
          CURRENT_HUGO=$(grep '^ARG HUGO_VERSION=' "${{ env.DOCKERFILE_PATH }}" | cut -d'=' -f2)
          CURRENT_RALPHEX=$(grep '^ARG RALPHEX_VERSION=' "${{ env.DOCKERFILE_PATH }}" | cut -d'=' -f2)

          echo "Current versions:"
          echo "  Bun: $CURRENT_BUN"
          echo "  Hugo: $CURRENT_HUGO"
          echo "  Ralphex: $CURRENT_RALPHEX"

          # Use input values or fall back to current Dockerfile values
          NEW_BUN=${INPUT_BUN_VERSION:-$CURRENT_BUN}
          NEW_HUGO=${INPUT_HUGO_VERSION:-$CURRENT_HUGO}
          NEW_RALPHEX=${INPUT_RALPHEX_VERSION:-$CURRENT_RALPHEX}

          echo ""
          echo "Target versions:"
          echo "  Bun: $NEW_BUN"
          echo "  Hugo: $NEW_HUGO"
          echo "  Ralphex: $NEW_RALPHEX"

          # Update versions in Dockerfile (GNU sed syntax for Linux)
          # Use | as delimiter to avoid breakage if version strings contain /
          sed -i "s|^ARG BUN_VERSION=.*|ARG BUN_VERSION=$NEW_BUN|" "${{ env.DOCKERFILE_PATH }}"
          sed -i "s|^ARG HUGO_VERSION=.*|ARG HUGO_VERSION=$NEW_HUGO|" "${{ env.DOCKERFILE_PATH }}"
          sed -i "s|^ARG RALPHEX_VERSION=.*|ARG RALPHEX_VERSION=$NEW_RALPHEX|" "${{ env.DOCKERFILE_PATH }}"

          # Verify changes
          echo ""
          echo "Updated versions in Dockerfile:"
          grep '^ARG BUN_VERSION=' "${{ env.DOCKERFILE_PATH }}"
          grep '^ARG HUGO_VERSION=' "${{ env.DOCKERFILE_PATH }}"
          grep '^ARG RALPHEX_VERSION=' "${{ env.DOCKERFILE_PATH }}"

          # Check if anything changed
          if git diff --quiet "${{ env.DOCKERFILE_PATH }}"; then
            echo "No changes detected - versions are already up to date"
            echo "updated=false" >> $GITHUB_OUTPUT
          else
            echo "Changes detected - versions updated"
            echo "updated=true" >> $GITHUB_OUTPUT
          fi

          echo "bun_version=$NEW_BUN" >> $GITHUB_OUTPUT
          echo "hugo_version=$NEW_HUGO" >> $GITHUB_OUTPUT
          echo "ralphex_version=$NEW_RALPHEX" >> $GITHUB_OUTPUT

      - name: Commit and push changes
        if: steps.update.outputs.updated == 'true'
        env:
          BUN_VERSION: ${{ steps.update.outputs.bun_version }}
          HUGO_VERSION: ${{ steps.update.outputs.hugo_version }}
          RALPHEX_VERSION: ${{ steps.update.outputs.ralphex_version }}
          GITHUB_ACTOR: ${{ github.actor }}
          ACTOR_ID: ${{ github.actor_id }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add "${{ env.DOCKERFILE_PATH }}"

          # Create commit with multi-line message using multiple -m flags
          git commit \
            -m "chore(ralphex-fe): update versions to Bun ${BUN_VERSION}, Hugo ${HUGO_VERSION}, Ralphex ${RALPHEX_VERSION}" \
            -m "Updated via GitHub Actions workflow dispatch." \
            -m "Co-Authored-By: ${GITHUB_ACTOR} <${ACTOR_ID}+${GITHUB_ACTOR}@users.noreply.github.com>"

          git push

          echo "✅ Dockerfile updated and pushed to repository"

      - name: No changes needed
        if: steps.update.outputs.updated == 'false'
        run: |
          echo "ℹ️ Dockerfile already has the requested versions - no update needed"

  build-and-push:
    needs: update-dockerfile
    # Build if versions changed and update_only is not checked
    if: ${{ needs.update-dockerfile.outputs.versions_updated == 'true' && inputs.update_only == false }}
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      # Pull latest changes since we just pushed
      - name: Pull latest changes
        run: git pull

      - name: Extract versions from updated Dockerfile
        id: versions
        run: |
          BUN_VERSION=$(grep '^ARG BUN_VERSION=' "${{ env.DOCKERFILE_PATH }}" | cut -d'=' -f2)
          HUGO_VERSION=$(grep '^ARG HUGO_VERSION=' "${{ env.DOCKERFILE_PATH }}" | cut -d'=' -f2)
          RALPHEX_VERSION=$(grep '^ARG RALPHEX_VERSION=' "${{ env.DOCKERFILE_PATH }}" | cut -d'=' -f2)

          if [ -z "$BUN_VERSION" ] || [ -z "$HUGO_VERSION" ] || [ -z "$RALPHEX_VERSION" ]; then
            echo "Error: Failed to extract version(s) from Dockerfile"
            echo "  Bun: ${BUN_VERSION:-<empty>}"
            echo "  Hugo: ${HUGO_VERSION:-<empty>}"
            echo "  Ralphex: ${RALPHEX_VERSION:-<empty>}"
            exit 1
          fi

          echo "bun=$BUN_VERSION" >> $GITHUB_OUTPUT
          echo "hugo=$HUGO_VERSION" >> $GITHUB_OUTPUT
          echo "ralphex=$RALPHEX_VERSION" >> $GITHUB_OUTPUT

          echo "Building with versions:"
          echo "  Bun: $BUN_VERSION"
          echo "  Hugo: $HUGO_VERSION"
          echo "  Ralphex: $RALPHEX_VERSION"

      - name: Generate image tags
        id: tags
        env:
          RALPHEX_VERSION: ${{ steps.versions.outputs.ralphex }}
        run: |
          NAMESPACE=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          BASE_IMAGE="${{ env.REGISTRY }}/${NAMESPACE}/${{ env.IMAGE_NAME }}"

          VERSION_TAG="${RALPHEX_VERSION}"

          TAGS="${BASE_IMAGE}:latest,${BASE_IMAGE}:${VERSION_TAG}"

          echo "namespace=$NAMESPACE" >> $GITHUB_OUTPUT
          echo "tags=$TAGS" >> $GITHUB_OUTPUT
          echo "Generated tags:"
          echo "$TAGS" | tr ',' '\n'

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.tags.outputs.namespace }}/${{ env.IMAGE_NAME }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ralphex-fe
          file: ${{ env.DOCKERFILE_PATH }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify image
        env:
          TAGS: ${{ steps.tags.outputs.tags }}
        run: |
          FIRST_TAG=$(echo "$TAGS" | cut -d',' -f1)

          echo "Verifying multiplatform image: $FIRST_TAG"
          docker pull --platform linux/amd64 "$FIRST_TAG"
          docker pull --platform linux/arm64 "$FIRST_TAG"

          echo "Verifying Bun installation (amd64)..."
          docker run --rm --platform linux/amd64 "$FIRST_TAG" bun --version

          echo "Verifying Hugo installation (amd64)..."
          docker run --rm --platform linux/amd64 "$FIRST_TAG" hugo version

          echo "Verifying Bun installation (arm64)..."
          docker run --rm --platform linux/arm64 "$FIRST_TAG" bun --version

          echo "Verifying Hugo installation (arm64)..."
          docker run --rm --platform linux/arm64 "$FIRST_TAG" hugo version

          echo "Verifying Chromium installation (amd64)..."
          docker run --rm --platform linux/amd64 "$FIRST_TAG" chromium-browser --no-sandbox --version

          echo "Verifying jq installation (amd64)..."
          docker run --rm --platform linux/amd64 "$FIRST_TAG" jq --version

          echo "Verifying PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH env var (amd64)..."
          docker run --rm --platform linux/amd64 "$FIRST_TAG" sh -c \
            'test "$PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH" = "/usr/bin/chromium-browser" && echo "OK: PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH is correct" || (echo "FAIL: expected /usr/bin/chromium-browser, got $PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH" && exit 1)'
          docker run --rm --platform linux/amd64 "$FIRST_TAG" sh -c \
            'test -x "$PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH" && echo "OK: binary is executable at $PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH" || (echo "FAIL: binary not executable at $PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH" && exit 1)'

          echo "Verifying PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD env var (amd64)..."
          docker run --rm --platform linux/amd64 "$FIRST_TAG" sh -c \
            'test "$PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD" = "1" && echo "OK: PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1" || (echo "FAIL: got PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=$PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD" && exit 1)'

          echo "Verifying ttf-freefont installation (amd64)..."
          docker run --rm --platform linux/amd64 "$FIRST_TAG" sh -c \
            'ls /usr/share/fonts/freefont/FreeSans.ttf && echo "OK: freefont found" || (echo "FAIL: freefont not found" && exit 1)'

          echo "Verifying Chromium installation (arm64)..."
          docker run --rm --platform linux/arm64 "$FIRST_TAG" chromium-browser --no-sandbox --version

          echo "Verifying jq installation (arm64)..."
          docker run --rm --platform linux/arm64 "$FIRST_TAG" jq --version

          echo "Verifying PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH env var (arm64)..."
          docker run --rm --platform linux/arm64 "$FIRST_TAG" sh -c \
            'test "$PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH" = "/usr/bin/chromium-browser" && echo "OK: PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH is correct" || (echo "FAIL: expected /usr/bin/chromium-browser, got $PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH" && exit 1)'
          docker run --rm --platform linux/arm64 "$FIRST_TAG" sh -c \
            'test -x "$PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH" && echo "OK: binary is executable at $PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH" || (echo "FAIL: binary not executable at $PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH" && exit 1)'

          echo "Verifying PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD env var (arm64)..."
          docker run --rm --platform linux/arm64 "$FIRST_TAG" sh -c \
            'test "$PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD" = "1" && echo "OK: PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1" || (echo "FAIL: got PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=$PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD" && exit 1)'

          echo "Verifying ttf-freefont installation (arm64)..."
          docker run --rm --platform linux/arm64 "$FIRST_TAG" sh -c \
            'ls /usr/share/fonts/freefont/FreeSans.ttf && echo "OK: freefont found" || (echo "FAIL: freefont not found" && exit 1)'

          echo "✅ Multiplatform image successfully built and pushed!"
          echo "Tags: $TAGS"
